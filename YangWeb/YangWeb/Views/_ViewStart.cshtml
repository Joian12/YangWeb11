@using Microsoft.AspNetCore.Http
@using YangWeb.Services; 
@{
    string userSession = UserSession.GetUserSession();
    //System.Diagnostics.Debug.WriteLine(userSession);
    if (userSession != null)
        Layout = "_LayoutUser";
    else
        Layout = "_Layout";
}
